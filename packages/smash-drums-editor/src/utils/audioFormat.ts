/** Audio entry inside a Smash Drums `.indies` package */
export const INDIES_AUDIO_FILE = "audio.ogg";

/** Album art inside a Smash Drums `.indies` package */
export const INDIES_COVER_FILE = "cover.png";

/** Menu preview clip inside a Smash Drums `.indies` package */
export const INDIES_PREVIEW_FILE = "preview.wav";

/** Official Indies cover art dimensions */
export const INDIES_COVER_SIZE = 500;

/** Official Indies preview clip length (seconds) */
export const INDIES_PREVIEW_SECONDS = 12;

/** Official Indies preview sample rate */
export const INDIES_PREVIEW_SAMPLE_RATE = 22050;

/** `MusicStream` in exported Clone Hero `notes.chart` */
export const CHART_MUSIC_STREAM = "song.ogg";